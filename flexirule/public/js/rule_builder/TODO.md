-   In Rule Builder, canvas for Condition Node after saving it show in canvas for true next and false link at the same position in false side.
-   side bar must be on right for LTR language and on left for RTL language.
-   for FieldPickerControl.vue it must show field label \_\_(label) instead of fieldname but storing fieldname.
-   clicking on node in canvas fastly opened the side bar.
-   must allow delete link line between node if we want to rearrange linking nodes.
-   fix show link result dropdown to be opened down link field directly Ex: (sub rule setting rule and for process_method)
-   must implement the test button for rule builder to test the rule.
-   [future] must implement backend and frontend show executions in ui in realtime.
-   backend must not allow linked to another rule (Which cyclic also link to the current rule ) in actions.
-   RUle Builder : on save the connectiong between starting node removed and make rest node as disabled?
-   removing all console log in rule builder.
